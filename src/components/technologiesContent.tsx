import React, { FC } from "react";
import Typed from "react-typed";
import HomeButton from "./home_button";
import CustomButton from "./project_header";

const TechnologiesScreenContent: FC = (props) => {
  return (
    <div id="techno" style={{ position: "relative", height: "500px" }}>
      TECHNO
    </div>
  );
};

export default TechnologiesScreenContent;
